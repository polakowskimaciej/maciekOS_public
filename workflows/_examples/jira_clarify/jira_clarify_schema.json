{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "jira_clarification",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "comment_body",
    "mode_used",
    "missing_data"
  ],
  "gate_status": {
    "type": "string",
    "enum": [
      "ready",
      "blocked"
    ],
    "description": "Indicates whether the story is ready for implementation"
  },
  "friction_level": {
    "type": "string",
    "enum": [
      "none",
      "low",
      "medium",
      "high",
      "critical"
    ]
  },
  "properties": {
    "comment_body": {
      "type": "string",
      "description": "Structured Jira Wiki Markup clarification comment"
    },
    "mode_used": {
      "type": "string",
      "enum": [
        "create",
        "append"
      ],
      "description": "Indicates whether the comment was newly created or appended"
    },
    "missing_data": {
      "type": "array",
      "description": "List of explicitly missing required information for clarification",
      "items": {
        "type": "string"
      }
    }
  }
}
